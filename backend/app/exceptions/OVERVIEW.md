# `backend/app/exceptions` Overview

This folder defines application-specific exceptions and their handling.

- `exceptions.py`: custom exception classes (HTTP-friendly).
- `exception_handler.py`: FastAPI exception handler that converts exceptions into JSON responses.

