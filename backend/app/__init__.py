"""
Leaf API Service (`backend/app/__init__.py`).

Purpose:
- Package-level metadata for the FastAPI backend.

How to read:
- `__version__` is used as the package version string.

Update:
- Bump `__version__` when releasing a new backend version.

Debug:
- If version mismatches appear in logs/clients, verify this file and where the value is surfaced.
"""

"""
Leaf API Service
"""

__version__ = "0.1.0" 