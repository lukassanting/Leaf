# `backend/app/storage` Overview

This folder contains storage implementations used by the backend.

Currently, it’s centered around file storage helpers (e.g. `file_storage.py`) which likely support persistence of assets/content outside the relational DB.

