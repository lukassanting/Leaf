# `backend/app/api/routes/database` Overview

Database endpoints and row APIs.

- `database_controller.py` defines CRUD for databases and CRUD for rows.
- Business logic (persistence, schema mapping, validation) lives in `backend/app/database/operations/database_operations.py`.
- DTOs live in `backend/app/dtos/database_dtos.py`.

