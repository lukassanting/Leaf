# `backend/app/dtos` Overview

This folder contains Pydantic DTOs (request/response schema types).

DTO responsibilities:

- Define what shapes are accepted by API endpoints.
- Define what shapes are returned back to the frontend.
- Keep controller code clean by centralizing validation/typing.

