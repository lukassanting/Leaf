# `frontend/src/lib` Overview

This folder contains non-React client utilities:

- API clients/types under `lib/api`
- Cache/mutation helpers (create/update leaf + priming cache)
- Warm-route helpers to improve perceived navigation performance
- Workspace defaults bootstrap helpers

