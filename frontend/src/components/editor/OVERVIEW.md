# `frontend/src/components/editor` Overview

This folder contains the editor surface and editor-specific widgets.

Key files:

- `LeafEditor.tsx`: the editor container component used by the editor route.

The editor likely combines:

- TipTap-based rich editing
- Markdown editing mode
- Slash / block menu + selection bubble (no static toolbar); status bar on the page shell

