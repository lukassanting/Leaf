# `frontend/src/app/(workspace)/graph` Overview

This route renders the **Leaf graph view**.

- `page.tsx` fetches the graph model and renders a lightweight SVG visualization.
- The view supports filtering by page title/path and navigates to the leaf editor when clicking nodes.

